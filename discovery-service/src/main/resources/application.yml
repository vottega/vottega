# eureka server port
server:
  port: 8761

spring:
  application:
    name: eureka-server
  docker:
    compose:
      enabled: false


eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    prefer-ip-address: true



